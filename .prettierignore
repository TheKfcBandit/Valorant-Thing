# Auto-generated by `cargo tauri` on every build. Prettier expectations
# don't survive regeneration, so any --write here would just be undone
# next build. They're also marked -text in .gitattributes to stop line-
# ending churn.
src-tauri/gen/schemas/

# Build / dependency dirs (already in .gitignore but listed here so
# explicit prettier glob invocations don't dig into them).
node_modules/
dist/
src-tauri/target/

# Lockfiles — managed by npm/cargo, not prettier.
package-lock.json
src-tauri/Cargo.lock
